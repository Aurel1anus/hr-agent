# 升级与数据安全

业务数据保存在 `backend/hr_recruiting.db`，简历文件保存在 `backend/data/`。更新源码时不得覆盖、删除或提交这两个位置。

## 首次启动

在两个 PowerShell 窗口分别运行：

```powershell
.\启动后端.ps1
```

```powershell
.\启动前端.ps1
```

## 更新版本

1. 关闭正在运行的前后端服务。
2. 用新版源码替换程序文件；保留本机的 `backend/hr_recruiting.db` 和 `backend/data/`。
3. 在项目根目录运行 `./更新.ps1`。
4. 运行 `./启动后端.ps1` 和 `./启动前端.ps1`。

若数据库存在未执行的 Alembic 迁移，系统会先把它复制到 `backend/backups/`，再执行迁移。迁移失败会停止启动，原数据库和备份都会保留。系统保留最近 30 份升级前备份。

## 发布检查

- 每次修改 SQLAlchemy 模型都必须新增 Alembic migration，不能修改已发布的 migration。
- 发布前分别用空数据库和上一版本数据库执行 `python -m alembic -c alembic.ini upgrade head`。
- 不使用 `drop_all`、重建数据库或删除历史业务记录作为升级方案。
- 需要恢复时，先停止服务，再用 `backend/backups/` 中对应的 `.db` 文件替换当前数据库。
